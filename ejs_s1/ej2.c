#include <stdio.h>
#include <stdlib.h>

int main(int argc, char** argv) {

    int ñx;
    int i;

    for ( i=0, i<10; i++)
    {
        printf("x=");
        scanf("%d",&x);
        printf("f(x) = %d\n",x[i]*x[i]);
    }


    return (EXIT_SUCCESS);
}
